# 原版静态基线

业务行为参考基线提交：`0dc8538 Update index.js`。

该提交用于说明原版行为与静态契约，不等同于 Phase 0 开始锚点或 Phase 1 进入回滚点；后两者见 `ROLLBACK_DRILLS.md`。

此文件记录模块化前必须保持的关键契约。它不能替代 SillyTavern 中的实际回归测试。

## 入口与生命周期

- `manifest.json` JavaScript 入口：`index.js`
- 异步 IIFE 启动，初始等待 1000ms
- `/phone` 斜杠命令及输入框拦截必须保留
- 全局入口：`window.__pmOpen`
- 启动日志包含：`[phone-mode] v9.5.7`

## 持久化契约

- IndexedDB 数据库：`PhoneModeDB`
- IndexedDB store：`kv`
- 历史主键：`ST_SMS_DATA_V2`
- 原存储键与迁移标记不得在纯模块化阶段更名

## CSS 契约

- 手机根选择器：`#pm-iphone`
- 遮罩选择器：`#pm-overlay`
- 模型列表：`.pm-model-options`
- 模型列表高度由 JavaScript 常量 `MODEL_VISIBLE_ROWS` 写入 CSS 变量，CSS 默认值为 4，每行高度为 `34px`
- 移动端媒体查询：`@media(max-width:500px),(max-height:700px)`

## 人工回归清单

- [ ] `/phone`、Quick Reply 与输入框拦截入口均可打开手机；最小化和关闭正常
- [ ] 单聊发送、AI 回复、刷新后历史恢复
- [ ] 群聊创建、编辑、发送和删除
- [ ] 主题、暗色模式、背景和头像裁剪
- [ ] 表情包导入与发送
- [ ] 拍一拍与双向记忆
- [ ] 使用既有非空数据验证旧聊天、群聊、表情、背景和设置可读取；刷新或重启后仍保持
- [ ] 数据导出、导入与备份恢复；恢复后刷新或重启复验
- [ ] 已登记的公开 `window.__pm*` 兼容入口在宿主完成初始化后可调用，且无初始化顺序异常
- [ ] 从 Phase 1 进入点回退到已登记锚点后，旧入口、旧数据、备份恢复和公开全局仍可用
- [ ] 浏览器控制台无新增错误

## Phase 0 宿主验证记录

- 状态：`blocked`（当前工作区未连接可控的真实 SillyTavern 宿主，禁止把自动检查冒充人工验收）。
- 待记录环境：SillyTavern 版本、浏览器版本、扩展部署路径、测试日期与测试数据范围。
- 每项结果必须记录 `通过/失败/阻塞`、复现步骤、控制台错误和必要截图；涉及持久化的项目还要在刷新或重启后复验。
- 完成全部项目后，才能勾选上方清单；`PHASE_GATES.md` 的两项宿主门禁均满足且 Phase 1 进入锚点独立登记后，才允许关闭 Phase 0。
