{
  "name": "tianyin-xiaojian-st",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Build tooling for the personal Tianyin Xiaojian project.",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --platform=browser --target=es2020 --outfile=index.js --legal-comments=none",
    "check:syntax": "node --check index.js",
    "check:ai": "node scripts/check-ai.mjs",
    "check:emoji": "node scripts/check-emoji.mjs",
    "check:behavior": "node scripts/check-behavior.mjs",
    "check:interactive": "node scripts/check-interactive.mjs",
    "check:pending": "node scripts/check-pending.mjs",
    "check:press": "node scripts/check-press-gesture.mjs",
    "check:ambient": "node scripts/check-ambient.mjs",
    "check:calendar": "node scripts/check-calendar.mjs",
    "check:budget": "node scripts/check-budget.mjs",
    "check:permissions": "node scripts/check-permissions.mjs",
    "check:contracts": "node scripts/check-contracts.mjs",
    "check:architecture": "node scripts/check-architecture.mjs",
    "check:lifecycle": "node scripts/check-lifecycle.mjs",
    "check:lifecycle-scope": "node scripts/check-lifecycle-scope.mjs",
    "check:memory": "node scripts/check-memory-budget.mjs",
    "check": "npm run build && npm run check:syntax && npm run check:ai && npm run check:emoji && npm run check:behavior && npm run check:interactive && npm run check:pending && npm run check:press && npm run check:ambient && npm run check:calendar && npm run check:budget && npm run check:permissions && npm run check:contracts && npm run check:architecture && npm run check:lifecycle && npm run check:lifecycle-scope && npm run check:memory"
  },
  "devDependencies": {
    "acorn": "8.15.0",
    "esbuild": "0.25.12"
  }
}
